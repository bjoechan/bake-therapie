# Bake Thérapie Showcase

A React and Vite cookie showcase site for Bake Thérapie, configured for Netlify.

## Local Development

```bash
npm install
npm run dev
```

## Netlify

Netlify can build this project with:

```bash
npm run build
```

The publish directory is `dist`, already configured in `netlify.toml`.
